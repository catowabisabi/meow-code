fix: Implement Session Persistence — save chat messages to DB, export/import/share sessions

## Context
Cato Claude holds chat messages in-memory via WebSocket. Sessions are lost on reload. Users cannot revisit previous conversations, export chat history, or share sessions. This is a critical UX gap for a professional AI coding tool.

## Your Task

### Step 1: Survey existing chat/message storage
Search for:
- api_server/models/ — existing message/session models
- api_server/routes/history.py — how history API works
- chatStore.ts — how messages are currently held in memory
- WebSocket chat.py — where messages enter the system

### Step 2: Design session/message DB schema
In api_server/db/models/ or a new migration:
- messages table: id, session_id, role, content (text), tool_call_id, timestamp, annotations(JSON)
- sessions table: id, title, created_at, updated_at, model, provider, metadata(JSON)

### Step 3: Persist messages on WebSocket receive
In api_server/ws/chat.py — on each message event:
- Write to messages table (async, fire-and-forget)
- Use session_id from the WebSocket session context

### Step 4: Session export API
Add to api_server/routes/sessions.py:
- GET /api/sessions/{session_id}/export — return full session JSON (messages + metadata)
- POST /api/sessions/import — accept JSON, recreate session + messages in DB

### Step 5: HistoryPage integration
In webui/client/src/pages/HistoryPage.tsx:
- Wire export button (download JSON)
- Wire import button (file input → POST /api/sessions/import)
- Show session list with timestamps

### Step 6: Commit
Commit keywords: density, iris, trajectory
Output: commit hash + brief description

ULW