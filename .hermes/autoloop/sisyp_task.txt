## Task: Add "Add note" annotation button to MessageBubble

**Context:**
MessageBubble.tsx already renders `paste_ref` annotations beautifully (line 573-585):
```
{message.annotations?.filter(a => a.type === 'paste_ref').map(ann => (
  <span>📎 {ann.content}</span>
))}
```
But there's NO UI to CREATE annotations. Users can't add notes to their messages.

**Your task:**
1. Read `webui/client/src/components/chat/MessageBubble.tsx` — focus on the isUser section around line 548-566 (the edit button area)
2. Read `webui/client/src/services/historyApi.ts` — confirm createAnnotation signature

3. Add an "Add note" button (+) next to the Edit button for user messages:
   - Small icon button (like `+` or 📝) that appears on hover, same pattern as Edit
   - On click: show an inline small input field (single line, like the annotation itself) with "Add note" placeholder
   - On submit (Enter or button): call `historyApi.createAnnotation(message.id, 'paste_ref', noteText)`
   - On cancel (Esc or blur): hide the input
   - After successful creation: show the new annotation immediately below the message meta (it'll be rendered by the existing code at line 573)

4. The annotation input should be a small inline form, not a modal. Think: "Add a note..." placeholder, submit button, cancel button — all inline with the message.

**Keywords:** annotation, contextual, sticky-note
**Source files:** `webui/client/src/components/chat/MessageBubble.tsx`, `webui/client/src/services/historyApi.ts`

Commit hash: f013a11

ULW