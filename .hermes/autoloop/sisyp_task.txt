## Task: Enable multi-language syntax highlighting in Monaco Editor

**Context:**
CodeModePage.tsx now has Monaco Editor but is hardcoded to TypeScript language. The editor should support multiple languages based on context or user selection.

**Your task:**
1. Read `webui/client/src/pages/CodeModePage.tsx` — understand the current Monaco integration
2. Add language auto-detection logic:
   - Parse `input` content to detect language (e.g., `#!/usr/bin/python` → python, `fn main()` → rust, `package main` → go, `function`/`const`/`let` → typescript/javascript)
   - Add a language selector button or detect from file extension if attach feature is implemented
3. Update the `Editor` component's `language` prop to use the detected language
4. Ensure the editor still handles single-line input behavior (Enter to send, Shift+Enter for newline)

**Keywords:** identity, attention span, anomaly detection
**Output:** commit hash + brief description after completing

ULW
