fix: Git diff view in Monaco — show staged/unstaged changes inline

## Context
CodeModePage.tsx uses Monaco Editor for code editing. The next phase adds Git integration to show diffs directly in Monaco (vs. a separate git panel).

## Your task
1. Read `webui/client/src/pages/CodeModePage.tsx` — understand current Monaco setup (Editor component, options, onMount pattern, toolbar)
2. Research Monaco's built-in diff editor API: `monaco.editor.createDiffEditor()` — it creates a side-by-side diff view
3. Implement a toggle in the CodeModePage toolbar to switch between:
   - **Edit mode** — normal Monaco code editor (current behavior)
   - **Diff mode** — show staged/unstaged changes using DiffEditor

Implementation plan:
   a. **Toolbar toggle** — add a "Diff" button in the editor toolbar that switches mode
   b. **DiffEditor component** — use `monaco.editor.createDiffEditor()` to show:
      - Left: original (HEAD or working tree baseline)
      - Right: modified version
   c. **Git status detection** — read git status via `child_process.exec('git status --porcelain')` and `git diff --name-only`
   d. **File selection** — if multiple changed files, show a dropdown to pick which file to diff
   e. **Read file content** — use `git show HEAD:path` for original, fs.readFile for working copy

Editor options for diff:
   - `renderSideBySide: true` (default, can toggle)
   - `readOnly: true` for both sides
   - `theme: 'vs-dark'`

4. Add a small "Diff" badge/button in the editor toolbar with icon

## Keywords: git, monaco, inline-diff
## Output: commit hash + brief description after completing

ULW
