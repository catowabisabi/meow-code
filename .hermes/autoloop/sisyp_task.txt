fix: Implement Agent Introspection Dashboard — real-time reasoning state HUD

## Context
Cato Claude's agent emits stream events (stream_thinking_delta, token usage, etc.) but UI only shows spinner. Need a live HUD panel showing agent reasoning state.

## Your Task

### Step 1: Find agent state events
Search for:
- stream_thinking_delta, token usage events in api_server/ws/
- WebSocket message types in webui/client/src/lib/websocket.ts
- Any existing agent_state, thinking events

### Step 2: Create IntrospectionStore
In webui/client/src/stores/ — create introspectionStore.ts:
```typescript
interface IntrospectionState {
  tokenBudgetUsed: number      // tokens consumed
  tokenBudgetTotal: number     // context window limit
  recursionDepth: number       // current loop/recursive depth
  selfReflectionCount: number  // times agent has self-reflected
  thinkingContent: string      // latest thinking stream content
  contextPressure: number       // 0-100% context fill
  status: 'idle' | 'thinking' | 'tool-use' | 'error'
}
```
Use existing WebSocket message handlers to update state in real-time.

### Step 3: Create IntrospectionPanel component
webui/client/src/components/IntrospectionPanel.tsx:
- Compact HUD (not full sidebar) — maybe a bottom bar or floating pill
- Show: token budget bar (used/total), recursion depth counter, context pressure %
- Expandable to show thinking content ticker (last N tokens of thinking stream)
- Auto-collapse when agent is idle for >5s
- Distinct from chat pane — clearly secondary/overlay

### Step 4: Wire into ChatPage
Add IntrospectionPanel to ChatPage.tsx — position it above or below message list.
Only show when agent is actively working.

## Verification
- Load app, start agent task — HUD appears with live numbers
- Token count increments as response streams
- Recursion depth shows if agent loops
- No "placeholder" text in component

## Commit keywords: tickermirror, context pressure, recursion depth
## Output: commit hash + brief description

ULW
