fix: Add LSP features to Monaco Editor in CodeModePage

## Context
Monaco Editor is integrated in CodeModePage.tsx with multi-language support.
The editor instance is stored in `monacoRef.current` via onMount.

## Your task
1. Read `webui/client/src/pages/CodeModePage.tsx` — understand the current Monaco integration (Editor component, monacoRef usage, current options)
2. The `monacoRef.current` gives you access to `monaco.editor.IStandaloneCodeEditor` which has rich LSP features

Add these enhancements:
   a. **Editor options tuning** — enable these in the `options` prop:
      - `glyphMargin: true` — show glyph margin for breakpoints
      - `bracketPairColorization.enabled: true` — colorize bracket pairs  
      - `suggest: { showKeywords: true, showSnippets: true }` — richer autocomplete
      - `quickSuggestions: { other: true, comments: false, strings: false }` — inline suggestions

   b. **Hover provider** — after the Editor mounts, register a hover provider:
      ```
      monacoRef.current?.monaco?.languages.registerHoverProvider('typescript', {
        provideHover: (model, pos) => {
          const word = model.getWordAtPosition(pos)
          return word ? { contents: [{ value: `**${word.word}**\n\nType info from LSP` }] } : null
        }
      })
      ```

   c. **Error markers** — show red squiggly underlines for demo:
      - Inject 2-3 fake type errors using `monacoRef.current?.monaco?.editor.setModelMarkers(model, 'typescript', [{...}])`
      - E.g., variable used before declaration, missing return type

3. Add a small "LSP" indicator badge in the editor toolbar showing features are active

## Keywords: breakpoint, doppler-shift, automatic trigger
## Output: commit hash + brief description after completing

ULW
